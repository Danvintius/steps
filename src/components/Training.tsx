export default class Training {
    id: number;
    date: number;
    dist: number

    constructor(id: number, date: number, dist: number) {
        this.id = id,
        this.date = date,
        this.dist = dist
    }
 }